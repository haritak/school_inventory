<%= form_with(model: primary_photo, local: true) do |form| %>
  <% if primary_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(primary_photo.errors.count, "error") %> prohibited this primary_photo from being saved:</h2>

      <ul>
      <% primary_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
